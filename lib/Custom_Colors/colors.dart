//import 'package:flutter/material.dart';

// i'll add colors here
//from figma and then yoy can use it from here

import 'package:flutter/material.dart';

class AppColors{
  // Colors used in the App
  static final Color kcolorPrimary = Color(0xFF2D8DFB);
  static final Color ktextPrimary = Color(0xFF000000);
}

//class Size{
//  static double Height = MediaQuery.of(context).size.height;
//  static double Width = MediaQuery.of(context).size.width;
//}